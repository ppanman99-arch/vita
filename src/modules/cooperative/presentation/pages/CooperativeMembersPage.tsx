import AdminMembersPage from '../../../../pages/admin-members/page';

/**
 * CooperativeMembersPage
 * 
 * Wrapper component that renders AdminMembersPage
 * This maintains the modular structure while reusing existing member management functionality
 */
export default function CooperativeMembersPage() {
  return <AdminMembersPage />;
}
