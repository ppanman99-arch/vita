# Supabase Configuration (required for Live features)
# Get these values from your Supabase project dashboard: https://app.supabase.com
# Settings > API > Project URL and anon/public key
VITE_SUPABASE_URL=https://your-project-id.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# Optional: Enable real auth (AuthService + Supabase). If unset or false, login uses demo flow.
# Set to true in production when Supabase Auth is configured.
# VITE_USE_REAL_AUTH=false

# Optional: Show/hide demo features. When false, demo routes and menu items are hidden.
# VITE_SHOW_DEMO_FEATURES=true

# Optional: Payment gateway (URL to backend/Edge Function that creates payment and returns redirect URL).
# VITE_PAYMENT_GATEWAY_URL=

# Optional: E-contract API (URL to backend/Edge Function for contract creation/signing).
# VITE_ECONTRACT_API_URL=

# Gemini API Configuration (Optional)
# For image prompt generation using Gemini API
# Get your API key from: https://makersuite.google.com/app/apikey
VITE_GEMINI_API_KEY=your-gemini-api-key-here

# Gemini Imagen API Configuration (Optional)
# For direct image generation using Gemini Imagen (Nano Banana Pro)
# Can use the same API key as Gemini or a separate one
# Get your API key from: https://aistudio.google.com/app/apikey
VITE_IMAGEN_API_KEY=your-imagen-api-key-here

# Note: Copy this file to .env.local for local development
# cp .env.example .env.local
# Then update the values with your actual credentials
